<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:control="clr-namespace:Izone.Control"
             mc:Ignorable="d"
             x:Class="Izone.View.ListSingleInAlbumPage"
             Title="{Binding Name}">
    <Shell.SearchHandler>
        <control:SearchSingleHandler Placeholder="..."
                                     ShowsResults="True"
                                     x:Name="searchSingle"
                                     SearchBoxVisibility="Collapsible">
            <control:SearchSingleHandler.ItemTemplate>
                <DataTemplate>
                    <StackLayout Padding="8">
                        <Label Text="{Binding Name}"
                               FontSize="Medium"
                               FontAttributes="Bold"/>
                    </StackLayout>
                </DataTemplate>
            </control:SearchSingleHandler.ItemTemplate>
        </control:SearchSingleHandler>
    </Shell.SearchHandler>
    <RefreshView IsRefreshing="{Binding IsRefreshing}"
                 Command="{Binding RefreshListCommand}">
        <ListView ItemsSource="{Binding Singles}"
                  ItemSelected="ListView_ItemSelected">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Padding="8">
                            <Label Text="{Binding Name}"
                                   FontAttributes="Bold"
                                   FontSize="Medium"/>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </RefreshView>
</ContentPage>